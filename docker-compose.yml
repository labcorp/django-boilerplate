name: awesome_app

services:
  front:
    image: awesome_app/front
    build:
      context: .
      dockerfile: Dockerfile.dev
      target: front
    ports:
      - 5173:5173
    volumes:
      - ./:/app
      - /app/node_modules
    command: sh -c "npm run dev -- --host"

  app:
    image: awesome_app/app
    build:
      context: .
      dockerfile: Dockerfile.dev
      target: app
    ports:
      - 5678:5678
      - 8000:8000
    volumes:
      - ./:/app
      - /app/.venv
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=django-db
    command: "python -m debugpy --listen 0.0.0.0:5678 manage.py runserver 0.0.0.0:8000 --skip-checks"
    depends_on:
      - db
      - front
      - redis

  celery_worker:
    image: awesome_app/app
    build:
      context: .
      dockerfile: Dockerfile.dev
      target: app
    volumes:
      - ./:/app
      - /app/.venv
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=django-db
    command: "celery -A conf worker -l info"
    depends_on:
      - db
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - 6379:6379

  db:
    image: postgres:16-alpine
    volumes:
      - db:/var/lib/postgresql/data
    healthcheck:
      test: pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}
      interval: 5s
      timeout: 10s
      retries: 120
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}

volumes:
  db:
